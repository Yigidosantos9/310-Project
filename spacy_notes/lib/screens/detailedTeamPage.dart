import 'package:flutter/material.dart';

class TeamNamePage extends StatelessWidget {
  const TeamNamePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Detailed Team Page'),
      ),
    );
  }
}
